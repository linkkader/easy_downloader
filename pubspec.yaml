name: easy_downloader
description: Easy Downloader is a simple and easy to use plugin to download files from the internet.
version: 0.0.18-dev-03
homepage: https://github.com/linkkader/easy_downloader/blob/main/example/example.dart
repository: https://github.com/linkkader/easy_downloader

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^0.13.5
  #locale storage
  isar: ^3.0.5
  #logging
  logger: ^1.0.0
  chunked_stream: ^1.4.1


dev_dependencies:
  build_runner: ^2.1.8
  build_version: ^2.0.0
  isar_generator: ^3.0.2
  lints: ^2.1.0
  test: ^1.19.2
  flutter_lints: ^2.0.0

executables:
  easy_downloader:
